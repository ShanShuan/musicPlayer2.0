<RelativeLayout xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="56dp"
    android:padding="3dp"
    tools:context=".MainActivity" >

    <ImageView
        android:id="@+id/ivSearchPic"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="10dp"
        android:src="@drawable/f" />

    <TextView
        android:id="@+id/tvSearchName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/tvSearchSinger"
        android:layout_alignTop="@+id/ivSearchPic"
        android:layout_toRightOf="@+id/ivSearchPic"
        android:gravity="center_vertical"
        android:text="歌名歌名歌名" />

    <TextView
        android:id="@+id/tvSearchSinger"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/ivSearchPic"
        android:layout_alignLeft="@+id/tvSearchName"
        android:textSize="12sp"
        android:text="歌手名称" />
    
</RelativeLayout>